export const movingEnum = Object.freeze({
  todoCard: 'todoCard',
  prerequisiteCircle: 'prerequisiteCircle',
})

export const originTypeEnum = Object.freeze({
  todoCard: 'todoCard',
  prerequisite: 'prerequisite',
})

export const classStringEnum = Object.freeze({
  listid: 'listid',
  parent: 'parent',
  deck: 'deck',
})

export const currentDateChangeEnum = Object.freeze({
  back: 'back',
  forward: 'forward',
  today: 'today',
})
